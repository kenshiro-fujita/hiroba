<h3>本の詳細</h3>
<div class='row'>
  <%= render 'books/book_aside' %>
  <div class="col-sm-8">
  <h2>書評一覧ページ</h2>
  <font size="4"><p class="mb-3">この本に対する全書評を表示</p></font>
    <% if logged_in? %>
      <% if @user_review.nil? %>
        <!-- 投稿フォーム -->
        <%= render 'reviews/rev_form' %>
      <% else %>
        <div>
          既に書評を投稿済みです。
          <%= link_to "#{@user.name}さんのレビュー", book_review_path(@book, @user_review) %>
        </div><br>
      <% end %>
    <% else%>
      <p><b><a href="/signup">会員登録</a>すると書評を投稿できるようになります。</b></p>
    <% end %>
    <!-- この本に対する書評 -->
    <% if @book.reviews.any? %>
      <% @reviews = @book.reviews %>
      <%= render 'reviews/review' %>
    <% else %>
      <font size="4" class="mt-3">この本へは、まだ誰も書評を投稿していません。<br>
      最初の投稿をお願いします！！</font>
    <% end %>
  </div>
</div>
